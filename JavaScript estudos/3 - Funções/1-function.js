function speedUp(velocity, acceleration) {
    let newVelocity = velocity + acceleration 
    console.log("Nova velocidade: " + newVelocity)
}
    speedUp (60, 10)

    // declaração basica de uma função -->

    //onde "function -x-(-parametro-)"
    //é a estrutura basica de uma função, lembrando que não é executada até ser chamada na forma de:
    // function -x- ()